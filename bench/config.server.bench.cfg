---
# HOW benchmark server config — pure proxy overhead (gatekeepers off).
host : 127.0.0.1
port : 18080
timeout : 2000            # ms to wait for an idle WS connection
idletimeout : 60000       # ms before closing excess idle connections
secretkey : bench-secret
# Gatekeepers disabled so the delta measured is the proxy transport itself,
# not the IP/apikey checks. Re-enable to measure their cost separately.
#allowedhosts :
#allowips :
#apikeys :
